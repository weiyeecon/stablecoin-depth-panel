# 深度快照收集器（平铺版）：纯浏览器部署，约 10 分钟

**这一版专门解决"文件夹选不了"的问题：整个项目里没有任何子文件夹，全部文件都在同一层。**
你只需要「全选文件 → 拖上去」，不涉及任何文件夹操作。

运行时需要的 `data/`、`panel/` 目录，程序会在 GitHub 服务器上自己创建。
论文里 7 月 8 日那个快照也已经平铺成 `seed_2026-07-08_...csv`，第一次运行时程序会自动把它放回正确位置，所以面板的第一个观测日仍然是 7 月 8 日。

依旧强调：**你不需要装 Python、不需要 VS Code、不需要命令行。代码跑在 GitHub 的服务器上，你的电脑可以全程关机。**

---

## 第 1 步：新建仓库

1. 打开 <https://github.com/new>
2. **Repository name**：`stablecoin-depth-panel`（随意）
3. 可见性选 **Public**（Actions 运行时长免费不限量；公开提交历史同时充当论文的第三方时间戳溯源）
4. **不要**勾选 "Add a README file"
5. 点 **Create repository**

---

## 第 2 步：上传文件（这次不会再卡住）

1. 解压 `depth_collector_flat.zip`，得到一个文件夹，**打开它**
2. 在 GitHub 空仓库页面点 **uploading an existing file**
3. 在解压出来的文件夹里 **Cmd+A 全选**（里面全是文件，没有文件夹，所以不会再有选不上的问题），拖到网页上
4. 等列表加载完，页面底部点 **Commit changes**

上传完成后，仓库首页应该能看到 15 个文件，包括 `collect_snapshot.py`、`puller_layer1.py`、`puller_layer2.py`、`corridors.csv` 等。

---

## 第 3 步：创建定时任务（复制粘贴一次）

1. 点仓库顶部 **Actions** 标签
2. 点 **set up a workflow yourself**（蓝色小字链接）
3. 编辑框里的示例内容**全选删掉**，粘贴下面这一整段：

```yaml
name: daily-depth-snapshot

on:
  schedule:
    - cron: "41 5 * * *"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: depth-snapshot
  cancel-in-progress: false

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 300
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - run: pip install -r requirements.txt

      - name: Collect snapshot
        env:
          CG_DEMO_KEY: ${{ secrets.CG_DEMO_KEY }}
        run: python collect_snapshot.py

      - name: Collect native L2 books
        if: always()
        continue-on-error: true
        run: python collect_exact_l2.py

      - name: Commit snapshot
        if: always()
        run: |
          git config user.name "depth-collector"
          git config user.email "depth-collector@users.noreply.github.com"
          git add -A data panel || true
          if git diff --cached --quiet; then
            echo "nothing to commit"
            exit 0
          fi
          git commit -m "depth snapshot $(date -u +%F) [skip ci]"
          for attempt in 1 2 3; do
            git pull --rebase --autostash origin "${GITHUB_REF_NAME}" || true
            if git push origin "HEAD:${GITHUB_REF_NAME}"; then
              echo "pushed on attempt ${attempt}"
              exit 0
            fi
            sleep 5
          done
          echo "push failed after 3 attempts"
          exit 1
```

4. 右上角 **Commit changes...** → **Commit changes**

（文件名默认 `.github/workflows/main.yml`，不用改——用这种方式创建，就绕开了"点开头的文件夹拖不上去"的问题。）

---

## 第 4 步（可选）：免费 CoinGecko key

不做也能跑，只是慢 5 倍；Public 仓库不限量，所以可跳过。

1. <https://www.coingecko.com/en/api/pricing> → **Demo（免费）** → 注册 → 生成 API key
2. 仓库 **Settings** → **Secrets and variables** → **Actions** → **New repository secret**
3. Name 填 `CG_DEMO_KEY`（一字不差），Secret 粘贴 key → **Add secret**

---

## 第 5 步：手动跑一次确认（不能省）

1. **Actions** 标签 → 左侧 **daily-depth-snapshot** → 右侧 **Run workflow** → 绿色 **Run workflow**
2. 等几分钟（无 key 时约 20–40 分钟），刷新看到 **绿色对勾 ✓**
3. **最终确认**：回仓库首页 → 点进 `data` → `snapshots`，应该看到两个日期文件夹：
   - `2026-07-08`（论文里的种子快照，程序自动放回去的）
   - 今天的日期（新采集的）

看到这两个文件夹 = 部署成功，可以安心出发。

---

## 旅行期间

每天 UTC 05:41 自动跑（GitHub 忙时可能延后或偶尔跳过，正常）。失败会自动发邮件到你的 GitHub 注册邮箱。

**收到失败邮件不用管**——少一天只是面板里少一行，不影响后续采集，也不会丢已存下的数据（工作流已改成"中途出错也先把当天拿到的部分存下来再报错"）。只有**连续一周以上全失败**才说明数据源接口变了，回来告诉我。

---

## 回来之后

- `data/snapshots/YYYY-MM-DD/` — 每天原始数据 + sha256 清单（论文数据溯源）
- `panel/panel_depth_2pct.csv` — 走廊 × 日期 × 层 × 资产 的完整面板
- `panel/summary_depth_2pct.csv` — 每走廊的 median / p10 / p90 / 天数
- `panel/manual_supplement_rows.csv` — 手工核实的 Bitso 行（与自动面板分开存放）

论文稳健性段可以直接写：「在 2026 年 8 月 X 日至 9 月 Y 日的 N 个每日快照中，表 3 的走廊排序在 Z% 的日期上不变，深度中位数与 7 月快照相差 W% 以内。」

---

## 与原包的关系

平铺版只改了文件位置，**没有改任何测量逻辑**：两个采集脚本（原 `vendored/` 下的）原样搬到根目录并改名为 `puller_layer1.py` / `puller_layer2.py`，深度口径、方向判定、venue 分类全部不变。已实测验证：用 7 月种子重建面板得到与原包完全相同的 132 格骨架（24 格 observed / 42 格 api 未观测 / 66 格该层未采集），BRL 法币层 2% 深度 $8,605,733 逐格一致。
